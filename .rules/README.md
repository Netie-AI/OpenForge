# OpenForge Rules Mirror

This directory mirrors high-level governance rules in plain markdown.

Active Cursor enforcement lives in `.cursor/rules/*.mdc`.

Keep both directories aligned when a rule changes:

- `.rules/*.md` for human-readable policy docs.
- `.cursor/rules/*.mdc` for always-on Cursor rule injection.
